# tasks-tui

A keyboard-driven terminal front end for the [`tasks`](../tasks) tracker, built on
[Bubble Tea](https://github.com/charmbracelet/bubbletea). It shells out to the `tasks`
binary and reads its JSON; it never touches `tasks/*.md` itself.

Design: `docs/specs/`. Plans: `docs/plans/`.
